experiment:
  name: ???
  gpus: 1
  seed: 0
training:
  lr: 1e-4
  lr_scheduler: null
  finetune_from_checkpoint: null
  trainer:
    # val_check_interval: 230
    # log_every_n_steps: 230
    max_epochs: 100
    check_val_every_n_epoch: 1
    limit_val_batches: 1000
    # max_steps: 500000
    devices: ${experiment.gpus}
  checkpointing:
    monitor: "loss/total/val"
    save_top_k: 5
    mode: "min"
hydra:
  job:
    name: ${experiment.name}
    chdir: false
clearml:
  project_name: "OrienterNet"
  task_name: "orienternet_training"
  output_uri: true
  dataset_id: e5bce8143ec6423cac891a31f123bb9a